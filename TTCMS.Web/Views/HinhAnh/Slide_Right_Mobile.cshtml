@model TTCMS.Domain.QuangCao[]
@if (Model.Any())
{
    <div class="promo-row row visible-xs visible-sm">
        @foreach (var item in Model)
        {
            <a class="col-xs-6 col-sm-3" href="@item.LinkRedirec"><img src="@item.Images" alt="@item.Title" /></a>
        }
    </div>
}

